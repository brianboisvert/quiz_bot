import React from 'react';

const Answer = (props) => {

  return(
    <div>Hdwfegbhe</div>
    // <button onClick={e => props.advance(props['response'])}>
    //   {props['response']}
    // </button>
  )
}

export default Answer;
